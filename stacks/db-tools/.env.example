# =============================================================================
# DB-TOOLS STACK — Adminer + RedisInsight (optional, deploy only when needed)
# Copy to .env and fill in values before starting this stack.
# Databases stack must be running first.
# =============================================================================

# Domains
ADMINER_DOMAIN=adminer.example.com
REDIS_ADMIN_DOMAIN=redis.example.com

# Adminer
ADMINER_DEFAULT_SERVER=postgres
ADMINER_DESIGN=pepa-linha-dark

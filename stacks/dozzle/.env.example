# =============================================================================
# DOZZLE STACK — Docker log viewer (deploy only when debugging)
# Access via SSH tunnel — see README.md § "Accessing Internal Tools via SSH Tunnel"
# Copy to .env and fill in values before starting this stack.
# =============================================================================

# Host label shown in the Dozzle UI
DOZZLE_HOSTNAME=your-hostname

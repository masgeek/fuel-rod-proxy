# =============================================================================
# MAIL STACK — Mailpit SMTP dev relay
# Copy to .env and fill in values before starting this stack.
# =============================================================================

# Domain (Mailpit web UI)
MAILPIT_DOMAIN=mail.example.com

# Message limits
MAX_EMAILS=1000
MP_MAX_AGE=1h
MP_VERBOSE=false

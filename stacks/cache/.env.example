# =============================================================================
# CACHE STACK — Redis credentials
# Copy to .env and fill in values before starting this stack.
# =============================================================================

# Redis (prod) — leave empty to disable auth
REDIS_PASSWORD=

# Redis (dev) — leave empty to disable auth
REDIS_DEV_PASSWORD=

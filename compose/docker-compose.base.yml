networks:
  internal:
    name: internal
    external: false
  web:
    name: web
    external: true

volumes:
  maria_db:
#    driver: local
    name: maria-db-data
    external: true

  postgres-data:
    driver: local
    name: postgres-data

  agwise-pg:
    driver: local
    name: agwise-pg

  pg-admin:
    driver: local
    name: pg-admin

  drone-ci:
    driver: local
    name: drone-ci

  metabase-data:
    driver: local
    name: metabase

  redis-data:
    driver: local
    name: redis-data

  redis-insight:
    driver: local
    name: redis-insight

  smtp:
    driver: local
    name: smtp-dev

  n8n-data:
    driver: local
    name: n8n-data

  workflow-data:
    driver: local
    name: workflow-data


  sonarqube_data:
    driver: local
    name: sonarqube-data

  sonarqube_extensions:
    driver: local
    name: sonarqube-extensions

  sonarqube_logs:
    driver: local
    name: sonarqube-logs

networks:
  internal:
    name: internal
    external: false
  web:
    name: web
    external: true

volumes:
  maria_db:
    driver: local
    name: fuelrod_maria_db

  postgres-data:
    driver: local
    name: postgres-data

  agwise-pg:
    driver: local
    name: agwise-pg

  pg-admin:
    driver: local
    name: pg-admin

  drone-ci:
    driver: local
    name: drone-ci

  metabase-data:
    driver: local
    name: metabase

  redis-data:
    driver: local
    name: redis-data

  redis-insight:
    driver: local
    name: redis-insight

  smtp:
    driver: local
    name: smtp-dev

  n8n-data:
    driver: local
    name: n8n-data